#include<stdio.h>

int factorial(int num){
    if(number<1)
	return ;

}

int main(){
    printf("enter a no to find its factorial : ");
    int number;
    scanf("%d",&number);
    printf("%d",factorial(number));
    printf("\nbye\n");
}
